CREATE TABLE train(
    id VARCHAR(36) PRIMARY KEY,
    train_number VARCHAR(255),
    train_name VARCHAR(255)
);

INSERT INTO train(id, train_number, train_name) values('00000000', '0000', 'hayate-1');